import { SET_USER_PROFILE, REMOVE_USER_PROFILE } from "./../actions/index";

const masterReducer = (state = {}, action) => {
    switch (action.type) {
        default: return state;
    }
};

export default masterReducer;
